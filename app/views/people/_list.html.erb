<% if @people.size == 0 %>
  <p>No people for this pedigree</p>
<% else %>
<table class="tablesorter">
  <thead>
  <tr>
    <th>ISB Person ID</th>
    <th>Name</th>
    <th>Pedigree</th>
    <th>Gender</th>
    <th>Birth Date</th>
    <th>Death Date</th>
    <th colspan="3" class="nosort">Controls</th>
  </tr>
  </thead>
  <tbody>
<% @people.each do |person| %>
  <tr>
    <td style="width: 100px;"><%= link_to person.isb_person_id, person %></td>
    <td><%= person.full_collaborator %></td>
    <td><%= link_to person.pedigree.name, person.pedigree if person.pedigree %></td>
    <td style="width: 50px;"><%= person.gender %></td>
    <td style="width: 50px;"><%= person.dob %></td>
    <td style="width: 50px;"><%= person.dod %></td>
    <td><%= link_to 'Edit', edit_person_path(person) %></td>
    <td><%= link_to 'Delete', person, :confirm => 'Are you sure?', :method => :delete %></td><% end %>
<% if false %>
<td><%= link_to 'Show', person %></td>
  </tr>
<% end %>
</tbody>
</table>
<% end %>

<%= will_paginate @people %>
