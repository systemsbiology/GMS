<%= form_for(@sample_assay) do |f| %>
  <% if @sample_assay.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@sample_assay.errors.count, "error") %> prohibited this sample_assay from being saved:</h2>

      <ul>
      <% @sample_assay.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :id %><br />
    <%= f.text_field :id %>
  </div>
  <div class="field">
    <%= f.label :sample_id %><br />
    <%= f.text_field :sample_id %>
  </div>
  <div class="field">
    <%= f.label :assay_id %><br />
    <%= f.text_field :assay_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
