<%= form_for(@phenotype) do |f| %>
  <% if @phenotype.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@phenotype.errors.count, "error") %> prohibited this phenotype from being saved:</h2>

      <ul>
      <% @phenotype.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :id %><br />
    <%= f.text_field :id %>
  </div>
  <div class="field">
    <%= f.label :diseases_id %><br />
    <%= f.text_field :diseases_id %>
  </div>
  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :description %><br />
    <%= f.text_area :description %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
