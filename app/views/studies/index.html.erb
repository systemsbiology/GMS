<h1>Listing studies</h1>

<table>
  <tr>
    <th>Id</th>
    <th>Name</th>
    <th>Principle</th>
    <th>Collaborator</th>
    <th>Description</th>
    <th>Contact</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @studies.each do |study| %>
  <tr>
    <td><%= study.id %></td>
    <td><%= study.name %></td>
    <td><%= study.principle %></td>
    <td><%= study.collaborator %></td>
    <td><%= study.description %></td>
    <td><%= study.contact %></td>
    <td><%= link_to 'Show', study %></td>
    <td><%= link_to 'Edit', edit_study_path(study) %></td>
    <td><%= link_to 'Destroy', study, :confirm => 'Are you sure?', :method => :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Study', new_study_path %>
